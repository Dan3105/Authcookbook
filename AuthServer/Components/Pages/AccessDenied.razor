@page "/access-denied"
@* Design-only Access Denied page for Blazor *@

<div class="access-denied-container" role="alert" aria-live="polite">
    <div class="ad-card">
        <div class="ad-icon" aria-hidden="true">
            <!-- Lock SVG -->
            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" class="lock-svg">
                <defs>
                    <linearGradient id="g1" x1="0" x2="1" y1="0" y2="1">
                        <stop offset="0" stop-color="#4f46e5"/>
                        <stop offset="1" stop-color="#06b6d4"/>
                    </linearGradient>
                </defs>
                <rect x="2" y="10" width="20" height="12" rx="2" stroke="url(#g1)" stroke-width="1.2" fill="rgba(79,70,229,0.06)"/>
                <path d="M8 10V8a4 4 0 018 0v2" stroke="url(#g1)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                <circle cx="12" cy="16" r="1.2" fill="#0ea5a4"/>
            </svg>
        </div>

        <h1 class="ad-title">Access Denied</h1>
        <p class="ad-message">
            You don’t have permission to view this page. If you believe this is an error,
            contact your administrator for access.
        </p>

        <div class="ad-actions">
            <a href="/" class="btn-primary">Go to Home</a>
            <a href="mailto:admin@example.com" class="btn-link">Contact Administrator</a>
        </div>
    </div>
</div>

<style scoped>
.access-denied-container {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 66vh;
    padding: 2rem;
    background: linear-gradient(180deg, rgba(15,23,42,0.02), transparent 60%);
}

.ad-card {
    width: 100%;
    max-width: 640px;
    background: linear-gradient(180deg, #ffffff, #fbfdff);
    border-radius: 14px;
    box-shadow: 0 10px 30px rgba(2,6,23,0.08);
    padding: 2.25rem;
    text-align: center;
    border: 1px solid rgba(15,23,42,0.04);
}

.ad-icon {
    width: 88px;
    height: 88px;
    margin: 0 auto 1rem auto;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(79,70,229,0.08), rgba(6,182,212,0.04));
}

.lock-svg {
    width: 56px;
    height: 56px;
}

.ad-title {
    margin: 0.5rem 0 0.25rem 0;
    font-size: 1.5rem;
    font-weight: 600;
    color: #0f172a;
}

.ad-message {
    margin: 0 0 1.25rem 0;
    color: #475569;
    line-height: 1.45;
    font-size: 0.98rem;
    max-width: 44rem;
    margin-left: auto;
    margin-right: auto;
}

.ad-actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
    margin-top: 0.5rem;
}

.btn-primary {
    display: inline-block;
    padding: 0.6rem 1.1rem;
    background: linear-gradient(90deg,#4f46e5,#06b6d4);
    color: #fff;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    box-shadow: 0 6px 18px rgba(6,182,212,0.08);
    transition: transform .08s ease, box-shadow .12s ease;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 30px rgba(79,70,229,0.08);
}

.btn-link {
    color: #475569;
    text-decoration: none;
    font-weight: 500;
    padding: 0.4rem 0.6rem;
    border-radius: 6px;
}

.btn-link:hover {
    text-decoration: underline;
    color: #0f172a;
}

</style>